# Detect Non-DRY Logic

Detect Non-DRY Logic

## Usage

``` r
detect_near_dupes(inv, threshold = 40, bucket_width = 100)
```

## Arguments

- inv:

  Result from get_function_inventory

- threshold:

  Levenshtein distance limit (default 40)
